spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tasklist
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  sql:
    init:
      mode: always
      continue-on-error: false

security:
  jwt:
    secret: ZnV0bmpoZXBicGJyYnR2Zm4zMTE=
    access: 3600000
    refresh: 25920000